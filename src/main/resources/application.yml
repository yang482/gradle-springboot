server:
  port: 8080

spring:
  datasource:
    # TODO Type your info
    # ex)
    # driver-class-name: org.mariadb.jdbc.Driver
    # url: jdbc:mariadb://localhost:3306/test?characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/test?characterEncoding=UTF-8&serverTimezone=UTC

    hikari:
      # TODO Type hikaricp option
      # ex)
      # username: user_account
      # password: password
      username: username
      password: password
      maximum-pool-size: 10

---
spring:
  config:
    activate:
      on-profile: default,local

  devtools:
    livereload:
      enabled: true

  thymeleaf:
    cache: false